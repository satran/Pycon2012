tabstop=4       4 space indents
shiftwidth=4    use > and < to indent and unindent code
smarttab        uses the shiftwidth setting while inserting <TAB>s
expandtab       insert spaces instead of <TAB>s
softtabstop=4   to make the backspace sane
autoindent      self explanatory i guess :)
